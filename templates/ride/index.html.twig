{% extends 'base.html.twig' %}

{% block main %}
  <section class="my-5">
    <img src="" alt="photo VTT en groupe" />
    <div class="container">
      <div class="row">
        <div>
          <h1>Les derniers rides proposés</h1>
        </div>
      </div>
    </div>
  </section>

  <section class="my-md-5 px-md-5">
    <div class="container">
      <h2 class="col-12 text-center">Les sorties par chez toi !</h2>
      <div class="row">
        {% for ride in all_rides %}
          {% include '/partials/card_partial.html.twig' %}
        {% endfor %}
      </div>
    </div>
  </section>
{% endblock %}
